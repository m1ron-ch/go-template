import MediaLibrary from '@/widgets/MediaLibrary/MediaLibrary';
import React from 'react';

export const MediaPage: React.FC = () => {
  return (
    <div style={{ padding: '24px' }}>
      <MediaLibrary />
    </div>
  );
};

export default MediaPage;
